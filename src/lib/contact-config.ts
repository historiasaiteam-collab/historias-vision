// Contact / channel configuration. Kept out of visual components so real
// numbers, emails, and social handles can be dropped in without touching UI.
// DEMO CONTENT — all values intentionally empty until real details are
// provided. Empty strings render as non-interactive text in the UI.

export const CONTACT_CONFIG = {
  supportEmail: "",
  whatsappNumber: "", // e.g. "+62..."
  whatsappUrl: "",    // e.g. "https://wa.me/62..."
  officeAddress: "",
  instagramUrl: "",
  youtubeUrl: "",
  linkedinUrl: "",
} as const;
